# Screenshots

Planned sanitized screenshots:

- user question and grounded answer;
- source references;
- insufficient-context response;
- document ingestion status;
- service health view.

Use only synthetic, public or explicitly approved content. Redact user names, document titles, domains, addresses and tokens.
